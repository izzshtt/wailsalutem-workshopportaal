# Productie-secrets — kopieer dit bestand naar .env.production op de VPS
# en vul met echte waarden. NOOIT committen.
#
# Genereer SECRET_KEY met:   python3 -c "import secrets; print(secrets.token_urlsafe(48))"
# Genereer DB_PASSWORD met:  openssl rand -base64 24

# Flask
SECRET_KEY="vervang-mij-met-een-lange-random-string"
ADMIN_EMAIL="ilijap441@gmail.com"

# Externe API (optioneel, vul in indien gebruikt)
API_URL="https://api.hbo-ict.cloud"
API_KEY="jouw-api-key"

# Database (binnen Docker netwerk; 'db' is de service-naam)
DB_NAME="Workshopportaal"
DB_USER="workshopuser"
DB_PASSWORD="vervang-mij-met-sterk-wachtwoord"
DATABASE_URL="mysql+pymysql://workshopuser:vervang-mij-met-sterk-wachtwoord@db:3306/Workshopportaal"

# Image-tag die docker compose pullt (CI/CD overschrijft dit per deploy)
WEB_IMAGE="gitlab.fdmci.hva.nl:5050/propedeuse-hbo-ict/onderwijs/student-projecten/2025-2026/out-bim/faaciiweedee14/web:latest"
